package org.uno.cards;

import org.uno.game.Game;

public interface WildCardEffect extends CardEffectStrategy{
    // just as role of validation so it will prevent mix cards type e.g color with wildCardEffect
}
