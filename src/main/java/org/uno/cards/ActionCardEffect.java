package org.uno.cards;

public interface ActionCardEffect extends CardEffectStrategy{
    // just as role of validation so it will prevent mix cards type e.g color with wildCardEffect  as WildCardEffect interfaces

}
